# DEPRECATED: This file is no longer used.
# Dependency management has been migrated to NorthernMan54/Homebridge-Dependency-Bot
# See .github/homebridge-dependency-bot.json for the new configuration.
#
# This file is kept for reference but dependabot is disabled.
# The new dependency bot handles both npm packages and provides more
# granular control over release streams (stable, beta, alpha).

version: 2
updates: []